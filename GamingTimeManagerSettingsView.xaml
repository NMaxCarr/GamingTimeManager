<UserControl x:Class="GamingTimeManager.GamingTimeManagerSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Margin="15">
        <StackPanel Orientation="Horizontal" Margin="5">
            <Label Content="Time limit (minutes) : "/>
            <TextBox TextWrapping="Wrap" Text="{Binding Settings.GamingTimePeriodGoal}" PreviewTextInput="ValidationTextBox" Width="50" MaxLength="5"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="5">
            <Label Content="Period: "/>
            <ComboBox Width="120" ItemsSource="{Binding Settings.AvailableGamingSessionPeriods}" SelectedItem="{Binding Settings.GamingTimePeriod}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="5">
            <CheckBox Content="Warn me when time is over" IsChecked="{Binding Settings.NotifyOnTimeGoalReached}"/>
        </StackPanel>
    </StackPanel>
</UserControl>